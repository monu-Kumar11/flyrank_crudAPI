version: '3.8'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgres://postgres:dev@db:5432/tasks
  db:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=dev
      - POSTGRES_DB=tasks
